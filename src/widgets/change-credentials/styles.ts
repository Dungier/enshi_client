"use client";
import { styled, Box } from "@mui/material";

export const StyledTopContainer = styled(Box)(({ theme }) => ({
  display: "flex",
  alignItems: "center",
  gap: 60,
  flexWrap: "wrap",
}));
