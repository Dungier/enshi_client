"use client";

import { styled } from "@mui/material";

export const StyledContainer = styled("div")(() => ({
  width: "100%",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  flexWrap: "wrap",
  gap: 50,
  padding: 40,
}));
